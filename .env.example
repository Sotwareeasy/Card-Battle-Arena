# Modo de la API: "development" o "production"
VITE_API_MODE=development

# URL del JSON Server en desarrollo
VITE_API_DEV_URL=http://localhost:3000

# URL del backend desplegado en Railway (se configura como variable
# de entorno DENTRO de Railway, no se escribe el valor real aquí)
VITE_API_PROD_URL=