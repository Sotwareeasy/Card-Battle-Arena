{
  "name": "card-battle-arena",
  "version": "1.0.0",
  "description": "Juego de cartas coleccionables inspirado en Harry Potter. Proyecto académico.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch src/data/db.json --port 3000",
    "start:server": "json-server --watch src/data/db.json --port $PORT --host 0.0.0.0",
    "start:frontend": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "json-server": "^0.17.4",
    "serve": "^14.2.3"
  }
}